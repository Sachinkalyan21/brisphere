import React from 'react';

const Header = () => (
  <header style={{ padding: '1rem', background: '#333', color: '#fff', textAlign: 'center' }}>
    <h1>Brisphere: Workation in Ladakh</h1>
  </header>
);

export default Header;
